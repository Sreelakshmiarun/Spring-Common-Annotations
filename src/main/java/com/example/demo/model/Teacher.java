package com.example.demo.model;

public interface Teacher {                             //In interface we can't give the @component annotation, only can give @Component annotation in this interface implemented classes only 
	
	void teach();

}                                                     //This interface implemented in TamilTeacher class
